import Logger, {COLOR, compute} from './module' //Logger импортирует дефолтное значение, может быть назван как угодно
import * as Module from './module.js'

console.log(compute(1, 2))
Logger.log(COLOR)
Module.default.log()